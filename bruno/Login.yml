info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/login/"
  body:
    type: json
    data: |-
      {
        "username": "tom@test.test",
        "password": "tom@test.test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
