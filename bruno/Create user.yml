info:
  name: Create user
  type: http
  seq: 3

http:
  method: post
  url: "{{baseUrl}}/api/register/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "user@my-domain.com",
        "password1": "hellohell0",
        "password2": "hellohell0"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
